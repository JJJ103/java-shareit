spring:
  config:
    activate.on-profile: test

  application:
    name: ShareItAppTest
  main:
    banner-mode: OFF

  datasource:
    url: jdbc:h2:mem:shareit
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
